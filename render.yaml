services:
  - type: web
    name: eitaa-ai-miniapp
    env: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port 
    envVars:
      - key: OPENAI_API_KEY
        sync: false
